let databaseConfig = {
      database_host: "mongodb://localhost:27017/Authentication_App_DB",    // as default port for mongod is 27017
      secret: "your secret"
};

module.exports = databaseConfig;